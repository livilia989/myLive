# frontend 환경변수 (API 키 같은 비밀 값은 절대 넣지 마세요. VITE_ 값은 브라우저에 그대로 노출됩니다)

# backend 주소. 비워 두면 같은 출처의 /api 를 사용합니다 (개발 서버는 vite proxy 로 8787 에 연결)
VITE_API_BASE_URL=

# true 면 backend 없이 브라우저 안에서 Mock LLM 으로 전체 흐름을 실행합니다.
# (설정 화면에서도 전환할 수 있습니다)
VITE_USE_MOCK_LLM=false
